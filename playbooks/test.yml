---
- hosts: all
  gather_facts: false
  roles:
  - hello
  tasks:
  - name: "test"
    foreman_organization:
      username: "admin"
      password: "foobar"
      server_url: "https://localhost"
      validate_certs: 0
      name: "MyOrg"
      description: "This is my org"
      label: "omit"
      state: present
